# Design Summary

- JS/TS + Python support; scan on open & save.
- Status bar + manual scan command.
- Distinct rule IDs/messages (securelintpro.*).
- Quick Fixes: env var replacement; add auth guard.
